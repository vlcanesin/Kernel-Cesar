_VETTAB:	equ	h0100

_kbhit:		equ	0
_getchar:	equ	2
_putchar:	equ	4

		org 0

main:
		mov #_getchar, r1
		jsr r7, (_VETTAB(r1))		; Espera e lê tecla digitada

;		mov r0, r5
;		mov #_putchar, r1
;		jsr r7, (_VETTAB(r1))		; Coloca tecla no visor

		HLT				; Fim